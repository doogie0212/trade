pusher = new Pusher gon.pusher.key,
  encrypted: gon.pusher.encrypted
  wsPort: gon.pusher.wsPort
  wssPort: gon.pusher.wssPort
  wsHost: gon.pusher.host
  httpHost: gon.pusher.host

window.pusher = pusher
